# ================================
# DJANGO SETTINGS
# ================================
SECRET_KEY=django-insecure-dev-key-change-this-in-production-12345
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-app.onrender.com,your-app.up.railway.app

# ================================
# DATABASE CONFIGURATION
# ================================
# For local development with Docker
DB_ENGINE=postgresql
DB_NAME=smart_trip_planner
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432

# For cloud deployment (Render/Railway)
# DATABASE_URL=postgresql://user:password@host:port/database

# ================================
# CORS SETTINGS
# ================================
# Frontend URLs (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://your-frontend.com

# ================================
# PRODUCTION DEPLOYMENT NOTES
# ================================
# 1. Generate a strong SECRET_KEY:
#    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
#
# 2. Set DEBUG=False for production
#
# 3. Add your deployment domain to ALLOWED_HOSTS
#
# 4. For Render/Railway, use DATABASE_URL instead of individual DB variables
#
# 5. Update CORS_ALLOWED_ORIGINS with your frontend URL

